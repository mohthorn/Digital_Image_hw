usage: ./pr01 'filename' 'base_image'
base_image is the image providing the hue.
base_image needs to have bigger or equal width and height than the original image.
The result will be stored as procedural_result.ppm in the folder.